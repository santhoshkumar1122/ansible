- name: variables form play level
  hosts: [localhost]
  vars_files:
  - variables.yaml
  tasks:
    - name: check variables
      ansible.builtin.debug:
        msg: "Hi, i am learning {{Course}}, my name is {{Name}}, course duration {{Duration}}"